name: summer_finals1
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true

  assets:
    - assets/peerpair1.png

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/peerpair1.png"
  remove_alpha_ios: true